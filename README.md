# WorkspaceAI
AI Agent for Workspace Automation


# Class 3 notes:
User signed in with Google in the browser -> send a token to the backend -> backend verified -> backend uses that token to call Calender API

To make backend verify work, we need the clinent ID from a prject in your Google Console, which is the credentials.json





